import React from "react";
import CustomButton from "../common/button/CustomButton";

const Heading = ({ title, url }) => {
  return (
    <div>
      <h2>{title}</h2>
      {/* <CustomButton title="Create New Community" url="" /> */}
    </div>
  );
};

export default Heading;
